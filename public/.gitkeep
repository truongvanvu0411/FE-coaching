# Next copies public/ into the standalone image, and git does not track empty
# directories — without this file a fresh clone has no public/ and the Docker
# build fails with "/app/public: not found".
